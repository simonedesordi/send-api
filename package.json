{
  "name": "send-api",
  "version": "0.0.1",
  "main": "lib/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "clean": "rm -rf lib",
    "watch-js": "./node_modules/.bin/babel src -d lib --experimental -w",
    "dev-server": "nodemon lib/app 9090",
    "dev-start": "npm run watch-js & npm run dev-server",
    "build": "npm run clean && ./node_modules/.bin/babel src -d lib --experimental",
    "start": "node lib/app 9090"
  },
  "author": "@simonedesordi",
  "dependencies": {
    "mongoose":"*",
    "restify": "4.0.0"
  },
  "devDependencies": {
    "babel": "^5.8.21",
    "babel-core": "^5.8.21",
    "nodemon": "^1.4.0"
  }
}